---
---

--- Before Day-3 ---
I already knew same as before
--- 

## Day-3 Checklist

- [ ] I can run a Python script using `uv run script.py` without setting up a local virtual environment
- [ ] I know where the temporary virtual environment is created when running the `uv add --script script.py pandas` command followed by `uv run script.py`
- [ ] I can create a new Python project using `uv init` and understand what `pyproject.toml` is used for
- [ ] I can add a dependency (e.g., `requests`) using `uv add` and see it reflected in the lockfile
- [ ] I can create a traditional virtual environment using `uv venv` and know when to use it
- [ ] I understand why installing packages globally with `pip install` is a bad habit
- [ ] I can open a project in VS Code, select the correct Python interpreter, and run code from the integrated terminal
- [ ] I know the difference between a `.py` script and a `.ipynb` notebook, and when to use each

--- After Day-3 ---
I learned these things
1)With uv No manual package installation is needed.
2) nano fast.py -->Create a file named fast.py,then writh something and ctrl+o to save.
3)ephemeral environment-->A temporary Python environment Created by uv for a specific task.environment is stored in uv’s cache.
4)/uv add --script script.py pandas/=> this tells “this script needs pandas”.it stores it inside the script (no manual install needed) then uv /run script.py/
5)/uv init my_project/--> Create the project(there is a fil named pyproject.toml)
6)pyproject.toml → (data/configuration) .shopping list + kitchen settings.The file doesn’t cook anything — it just describes what’s needed.
7) cat -->view file content
8)/uv venv/ -->creates a permanent (persistent) virtual environment.can be reused anytime.
9)first /cd my-project/ --> /code ./ -->This opens the whole folder in VS Code.
10)/.ipynb/-->Jupyter Notebook.A cell-based interactive environment.Instead of one long file, I run code in blocks (cells).

---

--- Feedback (Suggestions for the TDS Team) ---
This is my feedback ...same as before
---

---
---
